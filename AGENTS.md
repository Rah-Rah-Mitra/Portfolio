# AGENTS.md

Read [CLAUDE.md](CLAUDE.md) first — it is the canonical agent guide for this
repo, whatever model or harness you are.

Repo skills live in `.agents/skills/`: `impeccable` (frontend design work) and
`resume-editing` (any resume/CV change — mandatory reading before touching
resumes).

Hard rule: never hand-edit files under `public/resume/generated/` — they are
build artifacts of `scripts/resume/`.
