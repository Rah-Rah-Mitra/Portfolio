# Optical Courier reproducibility record

This directory is a planning record only. No Optical Courier concept sheet, 3D model, rig, animation, or production render has been generated for this checkpoint.

When generation begins, retain the source workflow/API JSON and enter its reviewed output, prompt, negative prompt, seed, model filename, SHA-256, source URL, license, dimensions, and output hash in `prompt-manifest.json`. Model weights and credentials remain local.
